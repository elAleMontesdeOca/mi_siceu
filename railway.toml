[build]
command = "npm ci && npm run build && composer install --no-dev --optimize-autoloader"

[start]
command = "php artisan serve --host=0.0.0.0 --port=$PORT"

[[build.files]]
source = "public/build"
destination = "/app/public/build"
